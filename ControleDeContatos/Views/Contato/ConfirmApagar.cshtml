@model ContatoModel
@{
    ViewData["Title"] = "Confirma apagar contato -";
}

<div class="text-center">
    <h3>Deseja realmente excluir o contato( @Model.Nome )?</h3>
</div>

<a role="button" class="btn btn-danger w-100" asp-route-id="@Model.Id" asp-controller="Contato" asp-action="Apagar">Confirmar exclusão</a>
<a role="button" class="btn btn-primary w-100 mt-1" asp-controller="Contato" asp-action="Index">Cancelar exclusão</a>